# Local LLM Chat

Little experiment to test the MediaPipe solution to run Gemma 2 directly in the browser

## Requirements
- Download [Gemma 2](https://www.kaggle.com/models/google/gemma-2/tfLite/gemma2-2b-it-gpu-int8) model then extract the `.bin` file at `public/models`